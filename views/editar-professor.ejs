<!DOCTYPE html>
<html>
<head>
    <title><%= id ? 'Editar' : 'Novo' %> Professor</title>
    <style>
        form { max-width: 500px; margin: 20px 0; }
        label { display: block; margin: 10px 0 5px; }
        input { width: 100%; padding: 8px; margin-bottom: 15px; }
        button { padding: 10px 20px; background-color: #007bff; color: white; border: none; cursor: pointer; }
    </style>
</head>
<body>
    <h1><%= id ? 'Editar' : 'Novo' %> Professor</h1>
    <a href="/professores">Voltar</a>
 
    <form action="/professores/salvar" method="POST">
        <% if (id) { %>
            <input type="hidden" name="id" value="<%= id %>">
        <% } %>
        
        <label>Nome:</label>
        <input type="text" name="nome" value="<%= professor ? professor.nome : '' %>" required>
        
        <label>Disciplina:</label>
        <input type="text" name="disciplina" value="<%= professor ? professor.disciplina : '' %>" required>
        
        <button type="submit">Salvar</button>
    </form>
</body>
</html>
 